# Audiobook UI

This directory hosts the Gradio-based control panel for the pipeline. The UI
depends on the shared `pipeline_common` models and orchestrator entry points.
Use `init_venv.ps1` to bootstrap a Python 3.11 virtual environment dedicated to
the app.
