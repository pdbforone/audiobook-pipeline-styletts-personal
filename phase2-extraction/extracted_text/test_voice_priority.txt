This is a test to verify that the CLI voice override takes priority over Phase 3 stored voices. When we pass Baldur Sanjin as the voice parameter, it should use that built-in XTTS voice instead of falling back to voice cloning. This is a test to verify that the CLI voice override takes priority over Phase 3 stored voices. When we pass Baldur Sanjin as the voice parameter, it should use that built-in XTTS voice instead of falling back to voice cloning.